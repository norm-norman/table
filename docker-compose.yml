version: "3.9"

services:
  backend:
    build:
      context: ./table-service
      dockerfile: Dockerfile.dev
    ports:
      - "5000:5000"
    volumes:
      - ./table-service:/app
      - /app/node_modules
    networks:
      - app-network

  frontend:
    build:
      context: ./table-client
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - ./table-client:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    networks:
      - app-network
    depends_on:
      - backend

networks:
  app-network:
    driver: bridge
